<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Contact Us - LUME</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Aboreto&family=Jost&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    body {
      background: #f8f5f2;
      color: #2c1810;
      font-family: 'Jost', sans-serif;
      margin: 0;
      padding: 0;
    }
    .contact-container {
      max-width: 900px;
      margin: 0 auto;
      padding: 3.5rem 1.5rem 2.5rem 1.5rem;
    }
    .contact-title {
      font-family: 'Aboreto', cursive;
      font-size: 2.5rem;
      color: #3a2414;
      margin-bottom: 2.5rem;
      letter-spacing: 1.5px;
      text-align: center;
    }
    .contact-section {
      margin-bottom: 2.2rem;
      text-align: center;
    }
    .contact-section h3 {
      font-family: 'Aboreto', cursive;
      color: #3a2414;
      font-size: 1.25rem;
      margin-bottom: 1rem;
      margin-top: 2rem;
    }
    .contact-section p {
      font-size: 1.08rem;
      color: #2c1810;
      line-height: 1.7;
      margin-bottom: 1.2rem;
    }
    .contact-form {
      max-width: 700px;
      margin: 2.5rem auto 0 auto;
      background: none;
      padding: 0;
    }
    .contact-form label {
      font-family: 'Jost', sans-serif;
      font-size: 1rem;
      color: #3a2414;
      margin-bottom: 0.5rem;
    }
    .contact-form input, .contact-form textarea {
      width: 100%;
      padding: 0.8rem 1rem;
      border: 1px solid #bdbdbd;
      border-radius: 8px;
      font-size: 1rem;
      margin-bottom: 1.2rem;
      font-family: 'Jost', sans-serif;
      background: #fff;
      color: #2c1810;
      transition: border-color 0.2s;
    }
    .contact-form input:focus, .contact-form textarea:focus {
      border-color: #3a2414;
      outline: none;
    }
    .contact-form textarea {
      min-height: 120px;
      resize: vertical;
    }
    .contact-form .btn {
      background: #2c1810;
      color: #fff;
      border: none;
      border-radius: 8px;
      padding: 0.7rem 2.5rem;
      font-size: 1.1rem;
      font-family: 'Jost', sans-serif;
      transition: background 0.2s;
    }
    .contact-form .btn:hover {
      background: #3a2414;
    }
    @media (max-width: 900px) {
      .contact-container {
        padding: 2rem 0.5rem 1.5rem 0.5rem;
      }
      .contact-title {
        font-size: 2rem;
        text-align: center;
      }
    }
  </style>
</head>
<body>
  <%- include("../partials/user/header") %>
  <div class="contact-container">
    <div class="contact-title">Get in Touch</div>
    <div class="contact-section">
      <p>Need help or have a question? Do reach out to us should you need assistance.</p>
      <p>For Order Status Please Email: <a href="mailto:lume.elegence@gmail.com" style="color: #3a2414; text-decoration: underline;">lume.elegence@gmail.com</a></p>
      <p>We're always happy to hear from you. Our client advisors are available:<br>
        Monday to Saturday 9 AM to 5 PM (IST)<br>
        Sunday 1 PM to 9 PM (IST)
      </p>
    </div>
    <div class="contact-section">
      <h3>Write to us</h3>
      <form class="contact-form" method="post" action="/contact">
        <div class="row g-3">
          <div class="col-md-6">
            <input type="text" name="name" placeholder="Name *" required>
          </div>
          <div class="col-md-6">
            <input type="email" name="email" placeholder="Email *" required>
          </div>
        </div>
        <input type="text" name="phone" placeholder="Phone number *" required>
        <textarea name="comment" placeholder="Comment*" required></textarea>
        <button type="submit" class="btn">Send</button>
      </form>
    </div>
  </div>
  <%- include("../partials/user/fooret.ejs") %>
  <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
  <% if (typeof success !== 'undefined' && success) { %>
    <script type="text/javascript">
      document.addEventListener('DOMContentLoaded', function() {
        Toastify({
          text: "Thank you! Your message has been sent to our team.",
          duration: 4000,
          gravity: "top",
          position: "center",
          backgroundColor: "#4CAF50",
          stopOnFocus: true
        }).showToast();
      });
    </script>
  <% } %>
</body>
</html> 